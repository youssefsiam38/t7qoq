version: 2

before:
  hooks:
    - go mod tidy

builds:
  - skip: true  # t7qoq is a library, not a binary

changelog:
  sort: asc
  filters:
    exclude:
      - '^docs:'
      - '^test:'
      - '^ci:'
      - Merge pull request
      - Merge branch

release:
  github:
    owner: youssefsiam38
    name: t7qoq
  draft: false
  prerelease: auto
  name_template: "{{.Tag}}"

announce:
  skip: true
